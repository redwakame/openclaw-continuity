# SHA256SUMS

Release ZIP checksums are generated per published release asset and listed in
the corresponding GitHub Release notes.

This repository file intentionally does not name local desktop archives.
